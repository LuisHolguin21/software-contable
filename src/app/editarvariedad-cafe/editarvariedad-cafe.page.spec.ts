import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditarvariedadCafePage } from './editarvariedad-cafe.page';

describe('EditarvariedadCafePage', () => {
  let component: EditarvariedadCafePage;
  let fixture: ComponentFixture<EditarvariedadCafePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(EditarvariedadCafePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
