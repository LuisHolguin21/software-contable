import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ernestoverdugo.proyectoContable',
  appName: 'proyectoContable',
  webDir: 'www'
};

export default config;
